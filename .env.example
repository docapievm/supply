# Copy to .env and fill values (do NOT commit real keys)
PORT=4000
DATABASE_FILE=./data/supply.db

# Provider API keys (set these via GitHub Secrets / .env)
ALCHEMY_API_KEY=
INFURA_PROJECT_ID=
INFURA_PROJECT_SECRET=

# Optional: preferred tokenlist URLs (comma-separated)
TOKENLIST_URLS=https://tokens.uniswap.org
